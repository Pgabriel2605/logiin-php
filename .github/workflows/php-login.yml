name: CI PHP Login

on:
  push:
    branches:
      - C-Master
  workflow_dispatch:

jobs:
  call-reusable-ci:
    uses: Pgabriel2605/php-workflowss/.github/workflows/login.yml@main
    with:
      php-version: '8.2'
    secrets:
      REPO_PAT: ${{ secrets.NEW_PAT }}  # aqui usamos o nome do secret que você criou
